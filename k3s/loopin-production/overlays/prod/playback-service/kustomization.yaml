apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: loopin-production

resources:
  - ../../../base/playback-service

images:
  - name: ghcr.io/sglee487/loopin-server/playback-service
    newTag: 9d34854f48600111e7d045871d7d4b2f84840275

configMapGenerator:
  - name: playback-service-config
    behavior: merge
    files:
      - application-prod.yml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: playback-service
